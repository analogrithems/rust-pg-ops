/// Handles downloading snapshots from S3
/// This is now just a placeholder as the functionality has been moved to the browser
#[allow(dead_code)]
pub struct SnapshotDownloader {}

impl SnapshotDownloader {
    #[allow(dead_code)]
    pub fn new() -> Self {
        Self {}
    }
}
